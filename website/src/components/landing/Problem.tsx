export default function Problem() {
  return (
    <section class="mx-auto w-full max-w-6xl px-4 pb-4 pt-16 sm:px-6 md:pb-8 md:pt-24">
      <div class="max-w-3xl">
        <h1 class="animate-rise font-display text-hero font-semibold tracking-tight">
          Turn a top-tier model into a director.
        </h1>
      </div>
      <p class="mt-5 max-w-prose text-lg leading-relaxed text-dim">
        Without structure, an expensive model spends its premium context writing every edit, every
        check and every micro-decision, and the direction of the work gets buried under the detail.
      </p>
      <p class="mt-4 max-w-prose leading-relaxed text-dim">
        Amale turns that model into a director instead of a laborer: it segments work into chunks
        with clear outcomes, delegates each chunk to cheap Flash models, and verifies direction only
        at chunk boundaries — the same verified delivery at a fraction of the token cost.
      </p>
      <p class="mt-6 max-w-prose leading-relaxed text-dim">
        The name is <span lang="fa" dir="rtl">عمله</span> — Persian in origin, pronounced{' '}
        <span class="text-base-content">AH-mah-lah</span>, meaning{' '}
        <span class="text-base-content">workers / laborers</span>. <em>Amale</em> is the project's
        Latin-script name: coordinated workers contributing to a shared, verified outcome.
      </p>
    </section>
  );
}
